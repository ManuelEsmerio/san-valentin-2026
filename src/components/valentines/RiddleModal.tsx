'use client';

// This component is no longer used and has been replaced by the standard MapModal -> KeywordModal flow.
// It is kept here to avoid breaking file references but its functionality is disabled.
export default function RiddleModal() {
  return null;
}
